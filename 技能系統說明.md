# 技能系統 / 技能升級 調整說明

寫給之後要改技能數值、加技能、加效果的人看。

---

## 1. 整體架構

```
技能資產（SK_*.asset）
├─ DamageSkillDefinition   → 造成傷害，可多段、可 AoE、可掛額外效果
├─ HealSkillDefinition     → 回血，升級可改成持續回血
└─ ComposableSkillDefinition → 只是一串 SkillEffectDefinition 的容器

效果資產（EFF_*.asset）
├─ DamageEffectDefinition     傷害
├─ HealEffectDefinition       回血 / 持續回血
├─ VulnerableEffectDefinition 易傷（目標受傷 +）
├─ WeakEffectDefinition       虛弱（目標輸出 −）
├─ ParalysisEffectDefinition  機率麻痺（讓敵人跳過行動）
└─ ApplyDebuffEffectDefinition 通用 debuff

升級
├─ SkillUpgradeManager（場景單例）  只記「哪些技能升過級」，RestartRun 重置
├─ SkillUpgradeDefinition（資產）    冥想面板上一張卡的顯示資料
└─ 每個技能／效果自己有 upgraded* 欄位，執行時讀 context.skillUpgraded 決定要不要套用
```

**關鍵觀念**：升級不是換一個新技能，是「同一個技能資產，多讀幾個 `upgraded*` 欄位」。
所以調升級效果 = 改資產欄位，**不用碰程式碼**。

---

## 2. 我要調某個技能的數值

先看這個技能是哪種資產（在 Project 視窗點開看 Inspector 最上面的 Script）：

### DamageSkillDefinition（背刺 / 雙擊 / 群體 / 麻痺 / 虛弱）

| 欄位 | 意思 |
|---|---|
| `Damage` | 每一下的傷害 |
| `Hit Count` | 攻擊次數（雙擊設 2） |
| `Hits All Enemies` | 打全體（群體打勾） |
| `Extra Effects` | 造成傷害後額外跑的效果（虛弱掛 EFF_Weak、麻痺掛 EFF_Paralysis…） |
| `Scale With Caster Attack Power` | 打勾才會 +角色攻擊力（目前全關，見第 7 點） |
| `Upgraded Bonus Damage` | 升級後每下 +多少傷害 |
| `Upgraded Bonus Hits` | 升級後 +多少攻擊次數 |

### HealSkillDefinition（補血 / 大補）

| 欄位 | 意思 |
|---|---|
| `Heal` | 立即回復量 |
| `Upgraded Bonus Heal` | 升級後 +多少立即回復 |
| `Upgraded Becomes Regen` | 打勾 → 升級後改成「持續回血」而非立即 |
| `Regen Per Turn` / `Regen Turns` | 持續回血：每回合回多少、持續幾回合 |

### ComposableSkillDefinition（易傷）

技能本身只有 `Effects` 清單，真正的數值在清單裡那些 EFF_* 資產上。
易傷 → 點開它的 `EFF_Vulnerable` 調。

---

## 3. 各效果資產的欄位

### EFF_Vulnerable（VulnerableEffectDefinition）易傷
| 欄位 | 意思 |
|---|---|
| `Stacks` / `Duration Turns` | 施加幾層、持續幾回合 |
| `Multiplier Per Stack` | 每層讓目標多受多少比例傷害（0.2 = +20%）※目前實際倍率寫死在 CombatActor，這欄只是顯示參考 |
| `Upgraded Bonus Stacks` | 升級後多施加幾層（群體用） |
| `Upgraded Bonus Duration` | 升級後延長幾回合（易傷用） |

### EFF_Weak（WeakEffectDefinition）虛弱
| 欄位 | 意思 |
|---|---|
| `Stacks` / `Duration Turns` | 施加幾層、持續幾回合 |
| `Upgraded Bonus Duration` | 升級後延長幾回合 |

減傷比例在 `CombatActor` 上：`Weak Reduction Per Stack`（每層 −20% 輸出）、`Weak Min Multiplier`（最多壓到 20%）。

### EFF_Paralysis（ParalysisEffectDefinition）麻痺
| 欄位 | 意思 |
|---|---|
| `Chance` | 命中機率（0.35 = 35%） |
| `Stun Turns` | 命中後暈幾回合 |
| `Upgraded Bonus Chance` | 升級後 +多少機率 |

---

## 4. 我要新增一個技能

1. Project 右鍵 → Create → GestureCombat → Skills → 選一種（Damage / Heal / Composable）
2. 填 `Skill Id`（**要唯一**，是技能的字串識別碼）、`Display Name`、`Icon`
3. 填數值 / 掛效果
4. 把這個資產拖進場景 `SkillLibrary` 元件的 `Skills` 清單
5. 讓某個法陣點位會觸發這個 skillId（看 `ISkillSlotProvider` 的設定，或手勢點的 `resolvedSkillId`）

---

## 5. 我要新增一種效果（例如「中毒」「流血」）

1. 寫一個 `XxxEffectDefinition : SkillEffectDefinition`，實作 `Apply(SkillExecutionContext context)`
2. 加 `[CreateAssetMenu(...)]`
3. 需要升級行為的話，加 `upgraded*` 欄位，在 `Apply` 裡讀 `context.skillUpgraded`
4. 需要新的持續狀態（每回合觸發）的話，在 `CombatActor` 加類似 `regens` 的清單 + 在 `OnTurnCleanup` tick
5. 建對應的 EFF_* 資產，掛到技能的 `Effects` / `Extra Effects` 清單

`context` 能拿到：`self`（施放者）、`target`（目標）、`allEnemies`（全體，AoE 用）、`skillUpgraded`、`report`、`gestureResult`

---

## 6. 冥想面板要多一個可升級項目

1. Create → Game → Skill Upgrade，建一個 `SkillUpgradeDefinition`
2. `Skill Id` 填**對應技能資產的 skillId**（攻擊 / 防禦這種非技能的，填 `攻擊` / `防禦`）
3. 填 `Display Name`、`Upgrade Description`、`Icon`
4. 拖進場景 `SkillUpgradePanelUI` 的 `Upgrade Catalog` 清單

冥想面板會自動生成卡片；已升過的顯示「已強化」，每次休息只能升一個。

---

## 7. 常見陷阱

- **不要多個技能共用同一個 EFF_Vulnerable 資產**：升級欄位會互相污染（群體想 +層數、易傷想 +回合，共用的話兩個都吃到）。群體另外用 `EFF_Vulnerable_Group`。
- **skillId 不能重複**：`SkillLibrary` 遇到重複只認第一個，後面的整個失效。
- **角色攻擊力（CombatActor.attackPower）目前對玩家沒作用**：普攻傷害來自場景 `GestureCombatActionHandler` 的 `Base Attack Damage`（固定 15）。要讓攻擊力生效，得打開 handler 的 `Scale Attack With Actor Power`，或打開技能的 `Scale With Caster Attack Power`。敵人的 attackPower 則是直接生效的。
- **攻擊 / 防禦升級的數值**在場景 `GestureCombatActionHandler` 元件上（`Attack Upgrade Bonus Damage` / `Defense Upgrade Bonus Per Point`），不是資產。
- **虛弱 / 易傷 / 麻痺 debuff 的持續回合**：敵人攻防是隔回合交替的，持續回合太短會蓋不到攻擊回合，建議至少 3。

---

## 8. 目前數值一覽（2026-09 平衡）

戰鬥基準：玩家 100 HP / 普攻 15一點 / 防禦 10一點；史萊姆 100 HP / 攻擊 20 / 韌性 3。

| 技能 | 基礎 | 升級後 |
|---|---|---|
| 背刺 | 10 傷害 | +6 → 16 |
| 雙擊 | 10 × 2 = 20 | 10 × 3 = 30 |
| 群體 | 9 全體 + 全體 1 層易傷 | 全體 2 層易傷 |
| 麻痺 | 35% 暈 1 回合 | 60% |
| 虛弱 | 敵輸出 −20%，3 回合 | 5 回合 |
| 易傷 | 敵受傷 +20%，3 回合 | 5 回合 |
| 補血 | +18 HP | +30 HP |
| 大補 | +35 HP 立即 | 每回合 +10，持續 3 回合 |
| 攻擊 | 15一點 | +5 → 20一點 |
| 防禦 | 10一點 | +5 → 15一點 |
